apiVersion: v1
kind: Service
metadata:
  name: web-global
spec:
  type: ClusterIP
  # type: LoadBalancer
  # externalTrafficPolicy: Local
  ports:
  - name: http
    port: 80
    targetPort: envoy
  - name: admin
    port: 9901
    targetPort: envoy-admin

